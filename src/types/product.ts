export type Product = {
    _id: string,
    name: string,
    price: string,
    description: any,
    materials: any,
    image:  any,
    type: string,
    quantity: number;
    totalPrice: number;
}